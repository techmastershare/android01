<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/page_bg"
    android:orientation="vertical" >

    <!-- Header -->
    <RelativeLayout
        android:id="@+id/layout_menu"
        android:layout_width="fill_parent"
        android:layout_height="50dp"
        android:background="@color/action_menu_bg"
        android:orientation="horizontal" >

        <ImageView
            android:id="@+id/logo_megatv"
            android:layout_width="80dp"
            android:layout_height="40dp"
            android:layout_alignParentLeft="true"
            android:layout_gravity="center"
            android:layout_margin="5dp"
            android:contentDescription="@string/app_name"
            android:scaleType="fitCenter"
            android:src="@drawable/logo_megatv" />

        <ImageView
            android:id="@+id/imageView1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerInParent="true"
            android:layout_marginRight="10dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ic_action_search" />
    </RelativeLayout>

    <!-- Hot Content -->
    <LinearLayout
        android:id="@+id/layout_hotcontent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="5dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/hot_content"
            android:textColor="@color/white"
            android:textSize="16dp"
            android:textStyle="bold" />

        <com.davidlee.scrollview.customviews.HorizontalListView
            android:id="@+id/list_hotcontent"
            android:layout_width="wrap_content"
            android:layout_height="80dp"
            android:layout_marginTop="10dp" >
        </com.davidlee.scrollview.customviews.HorizontalListView>
    </LinearLayout>
    
    <!-- New Content -->
    <LinearLayout
        android:id="@+id/layout_newcontent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="5dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/new_content"
            android:textColor="@color/white"
            android:textSize="16dp"
            android:textStyle="bold" />

        <com.davidlee.scrollview.customviews.HorizontalListView
            android:id="@+id/list_newcontent"
            android:layout_width="wrap_content"
            android:layout_height="80dp"
            android:layout_marginTop="10dp" >
        </com.davidlee.scrollview.customviews.HorizontalListView>
    </LinearLayout>
    
    <!-- List Category -->
    <ListView 
        android:id="@+id/list_category"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_margin="5dp"
        android:background="@color/white"
        android:cacheColorHint="#0000">
        
    </ListView>

</LinearLayout>